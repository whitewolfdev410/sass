import { Helmet } from "react-helmet";

const Login = () => {
  return (
    <section className="auth-wrapper">
      <Helmet>
        <title>Signin</title>
      </Helmet>

      <p>Login</p>
    </section>
  );
};

export default Login;
