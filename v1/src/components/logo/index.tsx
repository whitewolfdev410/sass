import LogoImg from "../../assets/logos/logo.png";

/**
 * Brand Logo for the Programme provider using the platform
 */

const Logo = () => <img src={LogoImg} style={{ maxWidth: "100%" }} />;

export default Logo;
